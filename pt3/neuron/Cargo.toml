[package]
name = "neuron"
version = "0.1.0"
edition = "2024"

[dependencies]
lazy_static = "*"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
bincode = "1"
uuid = { version = "1.22.0", features = ["serde", "v4", "v7", "fast-rng"] }
